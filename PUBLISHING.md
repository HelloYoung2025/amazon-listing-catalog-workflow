# 发布验收与本次修复

> 历史记录：下文对应单 Skill 版本 v2.0.1（`85e0280`）的复现与修复，予以保留。
> 后续完整套件修复已补入 A+ 6 个文件和 publish-gate 74 个文件，三套均在 `skills/`。
> 下文根目录安装命令、仅主入口 ZIP 与“配套技能不随包分发”的范围已被完整套件取代。
> 当前安装、升级、全套 ZIP 和验收步骤以 [README.md](README.md) 为准；历史测试结论不冒充新版验收。

## 本次复现

基线提交：`e9302b1dbfa81619d1d055c606251477cf474a53`。

用 Codex 自带 `install-skill-from-github.py` 在三个空白临时目录安装公开仓库：

| 输入 | 实际结果 |
|---|---|
| `--url https://github.com/HelloYoung2025/amazon-listing-catalog-workflow` | exit 1: `Missing --path for GitHub URL.` |
| `--repo HelloYoung2025/amazon-listing-catalog-workflow --path .` | exit 1: `Invalid skill name.` |
| 同上，增加 `--name amazon-listing-catalog-workflow` | exit 0，技能安装成功 |

这是确定的安装失败路径，但未收到反馈者的客户端版本与报错，不能断言每个用户都遇到了这两种错误。原来的完整 `git clone` 路径本身可用，原包 YAML 也通过技能校验；不能笼统声称整个仓库无法安装。

另有三项已确认的分发缺陷：

1. README 在仓库公开后仍要求私有仓库访问权限；没有解释根目录安装参数、ZIP 导入或客户端差异。
2. 菜单路由到未随包分发的 A+ 和 publish-gate 技能，没有在调用前检查可用性。
3. 内部 HTML 模板仍要求旧版 Bundle、协调器和回执参数，而本包没有这些程序；会产生错误依赖或虚假的已验证声明。

原始发布只检查文件数量、相对链接、源文件一致性、Git 推送和公开状态。没有做空白环境安装，也没有用随包模板完成一次独立渲染。问题在发布者验收不足，不应归咎于使用者。

## 修复

- 写出根目录技能的完整 Codex 安装参数及手动克隆路径。
- 提供顶层目录与技能名称一致的安装 ZIP，仅打包技能及其本地资源。
- 基础 Listing 流程无需外部技能；A+ 完整规划与正式校验明确是可选扩展，调用前先检查。
- 更换工作稿模板的旧版 Bundle 声明，移除协调器/回执区块，保留简单、可渲染的六类占位符。
- 删除无来源的历史规则日期示例，要求任务当日核实。
- 添加维护者校验脚本和 GitHub Actions。普通用户无需 Python 或 PyYAML。

## 每次发布的验收

1. 在干净环境安装 `requirements-dev.txt`，执行 `python scripts/check_release.py`；确认 YAML、资源链接、模板渲染、ZIP 解压和逐文件比对通过。
2. 检查该提交的 GitHub Actions 结果。失败时不发布对应 Release；不能用上一个提交的 PASS 代替。
3. 用 Codex 安装器的完整参数，额外指定 `--ref <本次完整提交 SHA> --dest <空白临时目录>`，验证实际远程安装；同名目录冲突必须明确报告，不能覆盖。
4. 发布 ZIP 与 SHA256SUMS 后，从 GitHub 重新下载，核对哈希和解压结构。仓库首页的 Source code ZIP 不代替安装 ZIP。
5. 修改流程或依赖时，检查首轮识别、范围选择、无子智能体降级和缺少 A+ 技能的路径。包格式检查不等于模型行为、文案质量或全部客户端兼容性测试。
6. Release 说明写明测试范围和未验证的宿主；收到新反馈时记录工具、版本、安装方式、报错及是否已进入技能流程。

现有 CI 检查的是包结构和模板可渲染性；远程安装与宿主行为仍需上述验收。它不能保证零错误，也不能把未经测试的平台写成“全面兼容”。
